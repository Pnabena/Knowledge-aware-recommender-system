# The static frontend requires no environment variables.
# The following settings apply only to the optional legacy live API.
RECOMMENDER_API_URL=http://127.0.0.1:8000
CORS_ORIGINS=https://your-app.vercel.app
